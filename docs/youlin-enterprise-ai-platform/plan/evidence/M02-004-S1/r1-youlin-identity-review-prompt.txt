This is a complete independent third-party LIGHT code review assignment. Do the review yourself; do not dispatch agents, edit, commit, push, run tests/services, or inspect credentials/.env/.git credentials. Read-only source inspection only. Do not invoke review orchestration.

Review dimensions: ai-coding-bad-habits, code-style, logic, business-logic, new-feature, reuse-architecture, performance, release-risk, security, compatibility, observability. No UI/UX surface or PR metadata is involved. No extension packs exist.

Scope: uncommitted Youlin identity persistence kernel since f4950e35d5, NOT the whole M0-M2 delivery. Nine additive draft tables, generated migration 0162, explicitly opt-in internal command boundary, pending HR-person registration, persistent revocation, synthetic real-PostgreSQL tests, Docker generation/verification tools. No product endpoint, HR sync, activation, identity binding/review, IdP worker or production use is claimed implemented. Those remain integration/release gates, not secretly considered done. MAX_SAFE_INTEGER is a terminal revocation version and all future activation/review paths must reject it. Actor is supplied by trusted server authentication, not a request-body authentication mechanism. Do assess correctness/security of implemented methods and additive migration. Do not demand implementing the entire backlog in this bounded kernel review.

Changes: read /tmp/youlin-identity-review.diff (2685 lines; continue reading until complete). It contains tracked and untracked source changes. Also read relevant surrounding files as needed. The generated snapshot packages/database/migrations/meta/0162_snapshot.json and docs/development/database-schema.dbml were generated by bun run db:generate in Docker, not handwritten; SQL and journal delta are in the diff. Avoid loading the entire large snapshot without a specific need. User-owned AGENTS.md changes are excluded and must not be attributed to this implementation.

Preparation: read ONLY the full Quick checklist section (including nested examples) of each listed dimension file, under .agents/skills/deep-review/references/dimensions/: ai-coding-bad-habits.md, code-style.md, logic.md, business-logic.md, new-feature.md, reuse-architecture.md, performance.md, release-risk.md, security.md, compatibility.md, observability.md. Read other sections only to clarify an ambiguous bullet.

Calibration: follow repo-specific rules, compare existing patterns; do not report widespread unchanged style as a defect. Security is not exempted by temporary lifespan. Findings must concern added lines and cite file:line evidence. Once concrete evidence exists, stop expanding. Do not audit unrelated existing code. Do not treat source inspection as runtime proof. Author reports 30 current identity tests, scoped types, selected lint and legacy 18 PostgreSQL/20 SQL/33 server tests passing in isolated Docker; validate code independently, not by trusting that claim.

Output ordinary Markdown. Each finding must state P0/P1/P2, introduced/pre-existing, likelihood high/medium/low, whether it blocks release, concrete location/evidence. Put unverified pre-deploy confirmation checks separately. If no findings, say so briefly. Explain missing integration gates separately from implemented-code defects. Report honestly if you cannot complete the scope.
