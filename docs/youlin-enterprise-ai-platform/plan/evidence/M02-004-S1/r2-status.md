# R2：身份生命周期增量（独立审查阻塞，未交付）

基线 `4985ab3295`；本轮源代码仍为未提交工作区。R1 的 manifest、审查结论与原始证据保持不变，不覆盖本轮。

## 实现与验证范围

- 提取共享控制面事务；保留企业锁→全局 principal 锁→当前授权重验、原子回执/审计/Outbox、安全诊断。终止 epoch 的 actor 不得继续执行。
- 私有服务 Outbox：数据库时钟、租约 token fencing、过期重领、退避、有限尝试及 dead 保留；ACK **不是** IdP 撤销完成证明。未实现实际 relay、Broker、DLQ 操作界面。
- 私有 HR 服务完整快照：稳定 person key、版本与内容矛盾隔离、跨法人/员工键冲突、任职历史、离职与返聘不恢复旧授权。新源版本但内容相同不重置凭据；人工禁用不会被 HR 快照解除。实际薪人薪事 wire adapter、可靠源版本及健康策略尚缺。
- 绑定候选：受信 issuer 白名单、当前 HR 锚点、全局 principal 保留；人工申请仅进入待复核，不自动绑定。未实现复核、MFA 衔接、激活或应用登录。
- 最终 Docker 身份套件 **62/62**：54 项真实 PostgreSQL、8 项 DTO；包括原 32 项及新增 30 项，不把重跑累计为新增用例。定向类型及 14 个修改/新增 TS 文件的分批 Lint 通过。
- 相关回归：server **33/33**，旧真实 PostgreSQL Reader **18/18**。容器退出与本轮资源清理有日志；不是全仓类型/构建或业务验收。
- 终止 epoch 负向回归：临时撤去 guard 后 **1 失败/40 通过**，失败点确为错误允许终止 actor；恢复后并新增租约时钟用例 **42/42**，最终扩展套件 **62/62**。早期日志对应逐步开发的源码，不能冒称其源码等同最终 hash。

## 独立审查阻塞

只读 Codex 初审在 90 秒截止，exit 124；随后独立 Pi 回退也在 90 秒截止，exit 124，未产生最终审查文本。两者均**不计通过**；没有以作者自审冒充独立审查，也未继续无限重试或提升预算。原始输出、输入、范围 diff 及最终源码 SHA-256 见本目录新增的 `r2/` 工件及 `r2-manifest.json`。

需要安排人工独立复核，或授权一次更长时限的独立静态审查；不需要扩大 Docker 测试资源。此轮不提交/推送、不标完成；未形成正式 Acceptance/UAT。执行账仍为原六个提前工程切片，M0～M2 的31项完整 Spec 状态不因本轮测试升级。
