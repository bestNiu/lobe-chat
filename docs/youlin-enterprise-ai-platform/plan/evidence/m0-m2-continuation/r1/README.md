# M0～M2 续建工程证据 R1

## 已完成的工程检查

- 身份86项（47+39，两片并集；78 PostgreSQL+8 DTO）；Server80项（桥接11、认证器9、撤权24、预算36）。
- 新部署工具11项，另有既有CI/参数选择器回归；真实Keycloak14、旧SQL20及旧PG Reader18项回归通过。
- 定向Lint和扩展后的身份入口类型闭包通过；不是全仓types。
- 最终源码重新完成六阶段离线构建、镜像打包、163迁移/重放、登录HTML及12个资源HTTP、合成哨兵备份恢复。最终本地候选镜像：`sha256:e06a78414614736d1388e71744be338eb729a58c354bb98ac435b55ebb690e14`。
- 所有本次Compose测试容器/网络/数据库卷清理已验证；候选镜像及私有原始构建目录保留。没有常驻员工实例。

## 独立审查

一次只读初审发现两项P2：临时ban过期语义、无效JWT误报/错误日志噪声。两项均先补失败回归，再修复。一次独立复核确认均解决且未发现新增问题。审查没有执行测试，不把静态审查冒充安全认证或产品UAT。

## 失败证据不覆盖

- `image-links-before`：pnpm相对symlink被复制成宿主绝对路径，镜像内找不到pg。修复后单测和真实镜像启动通过。
- runtime初轮因缺pg失败；下一轮恢复因ParadeDB模板已含schema失败。修复template0后重新验证通过。
- `shadow-before`第一片不包含新增Reader用例，退出0不是反例结果；`shadow-before-two`才是临时表遮蔽攻击失败证据。固定search_path后两片通过。
- `review-ban-before` / `review-jwt-before`为独立初审两项的失败回归；`reviewed-*`为修复后完整对应回归。
- Lint初次问题及自动格式化输出保留；未提高单次45秒身份测试预算或关闭隔离。

`manifest.json`冻结源文件与归档工件SHA-256；`verification/`存放冻结后的校验输出，不纳入自引用清单。旧R1～R4和预算证据未改写。真实配置及所有env文件均排除；原始构建/Compose目录私有保管，不上传公共Acceptance。

**边界：** 工具及默认关闭、未挂路由的身份内核工程验证。没有真实邮箱/员工号登录、Session/PEP、Chat、S3、SMTP、浏览器UAT、业务数据恢复或生产发布。首位管理员未创建；源码/依赖未整体冻结，不能跨提交混搭打包。M0～M2完整任务仍未完成，接入门见[续建记录](../../../16-m0-m2-continuation.md)。
