# SPEC-M05-010：示例能力

> designStatus: draft · deliveryStatus: not_started · 设计版本: 0.1（批次 2）
> 候选 A：AI Lead；实名 Owner：待认领；Tech/QA/Security/领域 Reviewer：待认领。
> 设计 W3～W5；首轮评审 W5～W6；Review W8；主里程碑 W10。切片计划不等于父 Spec 已完成。

## 1. 目标、故事与范围

作为 Pilot Owner，我要一套小而完整的能力包证明注册、审批、运行、权限与成本闭环。

逐项 FR：FR-D01、FR-E01、FR-F03、FR-H01；AC 分项：AC-06、AC-07、AC-08、AC-21。本项提供分项证据，不独立代表整个 AC 签收；主责见[追踪矩阵](../../02-traceability-and-delivery-gates.md)。

不纳入：未批准临床/受试者/人遗/PV/GxP 数据、外部生产开放、任意插件热装、生产高权限 Pi 执行、全量历史迁移。功能基线不因本设计扩大。

## 2. 依赖、切片与阻断

- 前置切片：[SPEC-M05-002](../M05/SPEC-M05-002.md)、[SPEC-M05-003](../M05/SPEC-M05-003.md)、[SPEC-M05-004](../M05/SPEC-M05-004.md)、[SPEC-M05-005](../M05/SPEC-M05-005.md)。
- 决策：D05, D06, D07, D10, D16，以[决策台账](../../04-unified-baseline-and-decision-register.md)为准；继承的决策按本项实际影响在 Review 中逐一确认。
- 共享合同：[K02](../contracts/K02-scope-and-authorization.md)、[K03](../contracts/K03-events-audit-and-jobs.md)、[K04](../contracts/K04-registry-review-and-execution.md)、[K06](../contracts/K06-context-and-memory.md)。
- 切片：W10 合成能力包；W15 C1 接入；W16 AC-21 真实评测。

依赖指所需模型/API/规则，不要求整个前置里程碑完工。跨里程碑最终集成由消费者联合验收；尚无真实依赖时仅可做合成数据 Spike，不开放生产旁路。

## 3. 状态、数据与一致性

CapabilityPackManifest{version,3to5SkillRefs,readOnlyToolRef,lowRiskWorkflowRef,2to3AgentRefs,fixtureVersion,evaluationRef,scope,approvalRefs}；精确依赖锁定，不为凑数复制同一能力。

所有字段为逻辑候选，尚非 Drizzle/迁移交付。必须补类型/可空/索引/复合 FK、唯一约束、版本与保留。Workspace/Project 引用不信任客户端；版本不可静默覆盖；生命周期按 D13，不臆造法定年限。

## 4. 接口、事件与交互

### 4.1 操作候选

导入→测试→评审→发布→合成 Smoke；Skill 覆盖导航/材料清单等低风险方法，Tool 只读，Workflow 固定版本；真实制度知识通过 M7 后再连接员工助手。

跨系统 OpenAPI/JSON Schema、内部运行时校验仍须下一步展开；操作名不是已上线 URL。变更用 expectedVersion/幂等键，事件用 K03 CloudEvents；Request ID/traceparent 贯穿但不授予访问权。

### 4.2 产品状态

目录说明用途/输入/边界/Owner；不可用依赖显示阻断和安全替代，不给用户公共模型旁路。

前后端共同验证 loading/empty/forbidden/error/expired/degraded/conflict；不适用状态需写原因。高风险确认必须说明影响，不能拿弹窗替代授权。

## 5. 授权、失败与安全

W10 只有 C0 时不得拿未审核手册直接 RAG 或用 Mock 假签 AC-21；Pi 可选试验不算首期必需示例能力。

统一错误信封 `{requestId,error:{code,message,retryable,details}}`；未认证 401、不可发现对象 404、版本/幂等冲突 409、输入不合法 422、限流 429；依赖失败与结果未知分开。重试资格取决于幂等/回执，不能仅因 HTTP 5xx 重放副作用。

## 6. 验证用例与证据

| Case ID | Given/When → Then 摘要 | 证据 |
| --- | --- | --- |
| SPEC-M05-010-T01 | 所有包对象有精确批准版本 → 可运行 | 合成夹具、请求/决定/状态/审计；待执行 |
| SPEC-M05-010-T02 | 缺任一批准 → 导入不等于可用 | 合成夹具、请求/决定/状态/审计；待执行 |
| SPEC-M05-010-T03 | 同包跨项目测试 → 权限隔离 | 合成夹具、请求/决定/状态/审计；待执行 |
| SPEC-M05-010-T04 | 固定 Workflow 重复运行 → 无意外副作用 | 合成夹具、请求/决定/状态/审计；待执行 |
| SPEC-M05-010-T05 | 合成评测 → 明确非真实业务验收 | 合成夹具、请求/决定/状态/审计；待执行 |

均为未执行用例。QA 还需补精确输入、前置数据、负向响应/状态、网络故障注入、清理步骤、版本与环境；测试必须注明合成/Mock 或真实 Provider。企业原文/Secret 不进 Git、公共 Acceptance 或未批准遥测。

## 7. 发布、迁移、运行与回退

W10 验能力框架/合成样本，W15 接 C1，W16 灯塔真实链；生产内容批准与安装包发布分开，失败停用包不删历史。

新 Schema 用 expand/migrate/contract，存量能力默认未批准；无法安全降级时前向修复。可观测含成功/失败/延迟/未知/撤权/预算/版本，不包含不必要原文。回退不得恢复已撤销访问或删除数据。

## 8. 实施拆分与 DoR 缺口

| 责任工作 | 必须补齐的产物 | 状态 |
| --- | --- | --- |
| Domain/Tech | 本项目标、切片边界、实名单 A、真实依赖与角色人日 | 待批准 |
| Backend/Integration | 现有仓库接点、物理 Schema/迁移、API/事件与状态机、契约夹具 | 逻辑草案，待可执行设计 |
| Frontend/Desktop | 页面/IPC/SSE/错误交互原型、键盘与跨端用例，或 N/A 理由 | 待细化 |
| QA/Security | 上述用例、威胁模型、边界/出站/并发/恢复证据 | 待执行 |
| Ops | Flag/灰度、限额/SLO/保留、故障与撤销手册、恢复/回退演练 | 待批准 |

当前 owner/schemaRefs/approvalEvidence 均未就绪。满足[DoR](../../05-spec-design-and-verification-details.md)后方可 approved；父 Spec 的早期切片完成不得推导全部 accepted。受控证据引用需版本、Hash、权限与批准人。

[机器索引](../catalog.json) · [本阶段目录](./README.md) · [设计工作区](../README.md)
