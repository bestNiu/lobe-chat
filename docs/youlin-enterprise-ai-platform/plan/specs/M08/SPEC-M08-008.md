# SPEC-M08-008：了解有临衔接

> designStatus: draft · deliveryStatus: not_started · 设计版本: 0.1（批次 4）
> 候选 A：Knowledge Owner；实名 Owner：待认领；Tech/QA/Security/领域 Reviewer：待认领。
> 设计 W9～W12；首轮评审 W12～W13；主里程碑 W16。切片计划不等于父 Spec 已完成。

## 1. 目标、故事与范围

作为业务Owner，我要平稳衔接“了解有临”现有机器人/渠道，避免两套身份和互相矛盾的知识。

逐项 FR：FR-G11、FR-K07；AC 分项：AC-21、AC-01。本项提供分项证据，不独立代表整个 AC 签收；主责见[追踪矩阵](../../02-traceability-and-delivery-gates.md)。

不纳入：未批准临床/受试者/人遗/PV/GxP 数据、外部生产开放、任意插件热装、生产高权限 Pi 执行、全量历史迁移。功能基线不因本设计扩大。

## 2. 依赖、切片与阻断

- 前置切片：[SPEC-M08-001](../M08/SPEC-M08-001.md)、[SPEC-M02-004](../M02/SPEC-M02-004.md)、[SPEC-M11-013](../M11/SPEC-M11-013.md)。
- 决策：D05, D06, D15，以[决策台账](../../04-unified-baseline-and-decision-register.md)为准；继承的决策按本项实际影响在 Review 中逐一确认。
- 共享合同：[K04](../contracts/K04-registry-review-and-execution.md)、[K05](../contracts/K05-resource-and-knowledge.md)、[K06](../contracts/K06-context-and-memory.md)、[K08](../contracts/K08-workbench-and-modules.md)。
- 切片：按上述前置合同先完成合成联调，再验证真实身份/授权/Provider；Mock 不签真实 AC。

依赖指所需模型/API/规则，不要求整个前置里程碑完工。跨里程碑最终集成由消费者联合验收；尚无真实依赖时仅可做合成数据 Spike，不开放生产旁路。

## 3. 状态、数据与一致性

ChannelInventory{channel,botOwner,identityMethod,corpusSource,entrypoints,usageEvidence,retirementPlan}；LegacyQuestionSample只保存批准的去标识聚合/合成重写，不整库复制聊天。

所有字段为逻辑候选，尚非 Drizzle/迁移交付。必须补类型/可空/索引/复合 FK、唯一约束、版本与保留。Workspace/Project 引用不信任客户端；版本不可静默覆盖；生命周期按 D13，不臆造法定年限。

## 4. 接口、事件与交互

### 4.1 操作候选

盘点→核验接口/域名/身份→批准知识归入统一发布→小组双跑评测→提示统一入口→按批准窗口退旧。企业微信通知/入口不默认具备安全会话与RAG权限。

跨系统 OpenAPI/JSON Schema、内部运行时校验仍须下一步展开；操作名不是已上线 URL。变更用 expectedVersion/幂等键，事件用 K03 CloudEvents；Request ID/traceparent 贯穿但不授予访问权。

### 4.2 产品状态

说明新旧入口、数据不自动迁移、功能差异和反馈方式；关闭旧入口保留安全导航，不能默默转发完整历史。

前后端共同验证 loading/empty/forbidden/error/expired/degraded/conflict；不适用状态需写原因。高风险确认必须说明影响，不能拿弹窗替代授权。

## 5. 授权、失败与安全

不按昵称/手机号合并机器人用户；匿名历史不能自动归个人记忆；未经批准渠道不得推送业务正文；无源系统能力证明则保留深链接而不是假接通。

统一错误信封 `{requestId,error:{code,message,retryable,details}}`；未认证 401、不可发现对象 404、版本/幂等冲突 409、输入不合法 422、限流 429；依赖失败与结果未知分开。重试资格取决于幂等/回执，不能仅因 HTTP 5xx 重放副作用。

## 6. 验证用例与证据

| Case ID | Given/When → Then 摘要 | 证据 |
| --- | --- | --- |
| SPEC-M08-008-T01 | 匿名旧用户 → 不自动关联员工 | 合成夹具、请求/决定/状态/审计；待执行 |
| SPEC-M08-008-T02 | 历史问题含个人信息 → 过滤/不导入 | 合成夹具、请求/决定/状态/审计；待执行 |
| SPEC-M08-008-T03 | 渠道不支持授权 → 不直接回答敏感内容 | 合成夹具、请求/决定/状态/审计；待执行 |
| SPEC-M08-008-T04 | 新旧答案冲突 → Owner裁决 | 合成夹具、请求/决定/状态/审计；待执行 |
| SPEC-M08-008-T05 | 切换失败 → 安全回退入口 | 合成夹具、请求/决定/状态/审计；待执行 |

均为未执行用例。QA 还需补精确输入、前置数据、负向响应/状态、网络故障注入、清理步骤、版本与环境；测试必须注明合成/Mock 或真实 Provider。企业原文/Secret 不进 Git、公共 Acceptance 或未批准遥测。

## 7. 发布、迁移、运行与回退

迁移仅针对获准内容与入口，完整历史迁移仍排除；发布/回退有Owner、窗口与用户通知，不伪造既有机器人实际接口。

新 Schema 用 expand/migrate/contract，存量能力默认未批准；无法安全降级时前向修复。可观测含成功/失败/延迟/未知/撤权/预算/版本，不包含不必要原文。回退不得恢复已撤销访问或删除数据。

## 8. 实施拆分与 DoR 缺口

| 责任工作 | 必须补齐的产物 | 状态 |
| --- | --- | --- |
| Domain/Tech | 本项目标、切片边界、实名单 A、真实依赖与角色人日 | 待批准 |
| Backend/Integration | 现有仓库接点、物理 Schema/迁移、API/事件与状态机、契约夹具 | 逻辑草案，待可执行设计 |
| Frontend/Desktop | 页面/IPC/SSE/错误交互原型、键盘与跨端用例，或 N/A 理由 | 待细化 |
| QA/Security | 上述用例、威胁模型、边界/出站/并发/恢复证据 | 待执行 |
| Ops | Flag/灰度、限额/SLO/保留、故障与撤销手册、恢复/回退演练 | 待批准 |

当前 owner/schemaRefs/approvalEvidence 均未就绪。满足[DoR](../../05-spec-design-and-verification-details.md)后方可 approved；父 Spec 的早期切片完成不得推导全部 accepted。受控证据引用需版本、Hash、权限与批准人。

[机器索引](../catalog.json) · [本阶段目录](./README.md) · [设计工作区](../README.md)
